<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mtkrawbridge"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="28"
        android:targetSdkVersion="35" />

    <!--
        LSPosed module declaration.
        xposedminversion: minimum API version this module requires.
        xposeddescription: shown in LSPosed Manager.
    -->
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:description="@string/app_description"
        android:icon="@drawable/ic_launcher"
        android:hasCode="true" >

        <!-- LSPosed module metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposeddescription"
            android:value="Bridges Samsung/MTK vendor camera tags to AOSP Camera2 RAW API. Fixes black viewfinder and enables RAW capture on LineageOS GSI + Helio G80." />

    </application>

</manifest>
